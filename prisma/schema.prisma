datasource db {
    provider = "postgresql"
    url      = env("POSTGRES_PRISMA_URL")
}

generator client {
    provider   = "prisma-client"
    output     = "./client"
    engineType = "client"
}

model Counter {
    id         Int @id @unique
    visitCount Int @default(0)
}
